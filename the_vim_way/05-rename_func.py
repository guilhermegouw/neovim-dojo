"""
Using the dot command, substitute every process_data with transform_data
"""

raw = "raw"
clean = "clean"


def process_data(data):
    return data


x = process_data(raw)
y = process_data(clean)
